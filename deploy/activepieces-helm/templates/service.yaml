apiVersion: v1
kind: Service
metadata:
  name: {{ include "yflow.fullname" . }}
  labels:
    {{- include "yflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yflow.selectorLabels" . | nindent 4 }}
