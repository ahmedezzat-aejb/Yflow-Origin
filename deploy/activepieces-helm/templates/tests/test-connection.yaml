apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yflow.fullname" . }}-test-connection"
  labels:
    {{- include "yflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
